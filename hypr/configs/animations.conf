#####################
### LOOK AND FEEL ###
#####################

# General settings
general {
    gaps_in = 3
    gaps_out = 5
    border_size = 2
    col.active_border = rgba(207,11,60,0.5)   # merah darah ke pink anime
    col.inactive_border = rgba(0,0,0,0)
    resize_on_border = true
    allow_tearing = false
    layout = dwindle
}

# Decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.7


    shadow {
        enabled = true
        range = 5
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # Blur
    blur {
        enabled = true
        size = 5
        passes = 2
        vibrancy = 0.1696
    }
}

# Animations
animations {
    enabled = yes, please :)

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1
    bezier = slide, 0.25, 1, 0.5, 1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear

# ===========================
# 🎞️ Preset Animasi Workspace
# ===========================

# 1. 🎬 Fade elegan (halus)
    # animation = workspaces, 1, 4.0, easeOutQuint, fade
    # animation = workspacesIn, 1, 3.5, easeOutQuint, fade
    # animation = workspacesOut, 1, 3.5, easeOutQuint, fade

# 2. 🛝 Slide kanan kiri (smooth swipe)
    animation = workspaces, 1, 4.0, slide, slide
    animation = workspacesIn, 1, 4.5, slide, slide
    animation = workspacesOut, 1, 3.5, slide, slide

# 3. 🚀 Slide cepat (responsive)
    # animation = workspaces, 1, 2.5, slide, slide
    # animation = workspacesIn, 1, 2.2, slide, slide
    # animation = workspacesOut, 1, 2.2, slide, slide

# 4. 🧊 Slide + fade (kombinasi)
    # animation = workspaces, 1, 3.2, slide, fade
    # animation = workspacesIn, 1, 3.0, slide, fade
    # animation = workspacesOut, 1, 3.0, slide, fade

# 5. 🌀 Pop-in klasik Hypr (default-ish)
    # animation = workspaces, 1, 3.5, easeInOutCubic, fade
    # animation = workspacesIn, 1, 3.0, easeInOutCubic, fade
    # animation = workspacesOut, 1, 3.0, easeInOutCubic, fade
    
}


dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}
master {
    new_status = master
}

misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}